<p align="center">
    <img src="https://i.imgur.com/NKsYBcg.png" width=700>
</p>

## 📝 Содержание
- [Цель работы](#Target)
- [Множество Мандельброта](#Mandelbrot)


## 📌 Описание работы <a name="Target"></a>
Я написал приложение на языке C++ с использованием библиотеки SFML, с помощью которого можно исследовать удивительную красоту множества Мандельброта, однако без оптимизации оно начинает притормаживать уже с самого запуска. В консоль печатается FPS (Frames Per Second - количество подсчитываемых за одну секунду кадров без учета времени отрисовки) после отрисовки каждого кадра.

Цель работы - исследовать возможности оптимизации точек для отрисовки множества Мандельброта, среди которых:
1) Оптимизация флагами компилятора (-O0, -O1, -O2, -O3, -Ofast)
2) Оптимизация с помощью массивов
3) Оптимизация с помощью intrinsic-функций


## ⭐ Множество Мандельброта <a name="Mandelbrot"></a>
Зададим последовательность $z_n = z_{n-1}^2 + z_0$ для $n \ge 2$, $z_0$ - фиксировано. Множество Мандельброта - множество таких точек $z_0$ на комплексной плоскости, что последовательность $z_n$ ограничена.

Эту рекурренту на данный момент математикам упростить не удалось, однако можно попытаться ускорить подсчёт при помощи аппаратных оптимизаций.

Пусть $z_n = x_n + i*y_n$, тогда \
$x_{n+1} = x_n^2 - y_n^2 + x_0$ \
$y_{n+1} = 2x_ny_n + x_0$

Будем считать, что точка принадлежит множеству Мандельброта, если спустя MAX_DOT_TRIES = 256 итераций, она всё ещё осталась в окружности радиусом 2 от начала координат. Доказано, что если $|z_n| > 2$ для некоторого $n$, то $|z_n| \rightarrow \inf$

<p align="center">
    <img src="https://i.imgur.com/4AhvyeH.png" width=200>
    <img src="https://i.imgur.com/rNqewnP.png" width=200>
    <img src="https://i.imgur.com/70EQteb.png" width=200>
</p>


## 💻 Оборудование и инструменты <a name="Equipment"></a>
Работа проводилась на ноутбуке, подключенном к электросети. \
Процессор: Intel Core I5-13420H \
Графический процессор (GPU) не использовался \
Многопоточность не использовалась, нагружалось одно ядро, но для оптимизации процессор переключал задачу с одного ядра на другое автоматически.


## 🚀 Ход работы <a name="Progress"></a>
Перед началом тестов необходимо любым образом разогреть процессор и дождаться включения ситемы охлаждения. Если этого не сделать, результаты с большоё вероятностью получаться неточные, так как температура процессора сильно влияет на троттлинг и скорость работы процессора.

Полученные результаты необходимо отфильтровать перед подсчётом среднего. Я решил отбрасывать значения, отличающиеся от среднего более, чем на среднеквадратичное, умноженное на 3:
```python
filter(lambda x: x - average <= deviation_x3, test_results)
```
По этим числам вновь считается среднее арифметическое, среднеквадратичное отклонение, стандартная ошибка среднего и погрешность.

### 1) Без оптимизаций и оптимизация флагами
Здесь каждая точка считается по отдельности

Код подсчёта одной точки:
```cpp
float x1 = x0;
float y1 = y0;

int n = 0;
for (; n < MAX_DOT_TRIES; ++n)      // MAX_DOT_TRIES см. в разделе "Множество Мандельброта"
{
    float x1_quad = x1*x1;
    float y1_quad = y1*y1;
    float x1_y1   = x1*y1;

    if (x1_quad + y1_quad >= MAX_DISTANCE_QUAD)
        break;

    x1 = x1_quad - y1_quad + x0;
    y1 = 2*x1_y1 + y0;
}
```

Полученные мной результаты:
```
Без оптимизации [-O0]:
Среднее время выполнения (в тиках): 103094460.65 +- 26696.21 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 265623.92
Погрешность измерений: 0.03%

С оптимизацией флагами [-O1]:
Среднее время выполнения (в тиках): 52546075.9 +- 37140.58 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 367672.96
Погрешность измерений: 0.07%

С оптимизацией флагами [-O2]:
Среднее время выполнения (в тиках): 4721548.4 +- 3838.64 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 38193.97
Погрешность измерений: 0.08%

С оптимизацией флагами [-O3]:
Среднее время выполнения (в тиках): 4713576.19 +- 1973.87 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 19440.33
Погрешность измерений: 0.04%

С оптимизацией флагами [-Ofast]:
Среднее время выполнения (в тиках): 4711592.44 +- 2002.71 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 19926.7
Погрешность измерений: 0.04%
```

Как видно, оптимизации -O2, -O3 и -Ofast почти не отличаются, так как наибольшая оптимизация была произведена ещё на этапе -O2, а дополнительные оптимизации данный код уже не меняют.


### 2) Оптимизация на массивах
Суть оптимизации: за одну итерацию цикла считать не одну точку, а несколько (я выбрал $n = 4$). Так как результаты следующей точки не зависят от результатов предыдщей, то такое действие должно привести к выполнению подсчётов параллельно, благодаря наличию у большинства современных процессороа нескольких математических модулей.

Поскольку -O0 и -O1 не подразумевают почти никакую оптимизацию, -O2 может опустить важные в данном случае оптимизации, а -Ofast может оказаться слишком рискованным и поменять ход работы программы, то оптимизацию на массивах я решил проводить с флагом -O3.

В теории оптимизатор компилятора может заметить, что используется массив из $2^k$ элементов типа float, и подставить ассемблерный код вместо моих инструкций векторные инструкции для mm128. Однако в результате тестов было обнаружено, что gcc не смог это провернуть и скорость работы программы значительно упала:
```
С оптимизацией на массиве (gcc):
Среднее время выполнения (в тиках): 22788394.95 +- 14068.38 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 138557.51
Погрешность измерений: 0.06%
```

В результате я решил попробовать использовать clang, и вот он уже дал существенный прирост скорости:
```
С оптимизацией на массиве (clang):
Среднее время выполнения (в тиках): 1178629.96 +- 914.82 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 9009.97
Погрешность измерений: 0.08%
```

Как и ожидалось, скорость работы программы увеличилась примерно в $n$ раз за счёт уменьшения количества итераций


### 3) Оптимизация при помощи intrinsics
Последним шагом в исследовании оптимизаций стало использование intrinsic-функций и векторных типов данных: __m128 и __m256. Я бы с радостью попробовал использовать __m512, однако в ходе исследования было выявлено, что, хотя раньше процессоры Intel Core 13-го поколения поддерживали инструкции AVX512, но, к сожалению или к счастью, Intel отключил поддержку для новых представителей микроахрхитектуры Raptor lake (13-е поколение Intel Core).

Результат оптимизации:
```
С использованием intrinsic-функций (mm128):
Среднее время выполнения (в тиках): 1179743.88 +- 1002.87 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 9927.91
Погрешность измерений: 0.09%

С использованием intrinsic-функций (mm256):
Среднее время выполнения (в тиках): 595553.65 +- 2754.2 (стандартная ошибка среднего)
Среднеквадратичное отклонение: 26985.49
Погрешность измерений: 0.46%
```

Как и ожидалось, код на __m128 сработал примерно за такое же время, что и потимизация на массиве, скомпилированная через clang,


## 📈 Итоги
Таблица всех результатов:

| Оптимизация                       | Среднее время работы (тики)   | Средний FPS   |
| :-------------------------------- | :---------------------------: | :-----------: |
| Без оптимизации [-O0]             | 103094460.65                  | 24            |
| Оптимизацией флагами [-O1]        | 52546075.9                    | 51            |
| Оптимизацией флагами [-O2]        | 4721548.4                     | 52            |
| Оптимизацией флагами [-O3]        | 4713576.19                    | 52            |
| Оптимизацией флагами [-Ofast]     | 4711592.44                    | 54            |
| Оптимизация на массивах (gcc)     | 22788394.95                   | 105           |
| Оптимизация на массивах (clang)   | 1178629.96                    | 129           |
| Intrinsics-оптимизация (mm128)    | 1179743.88                    | 180           |
| Intrinsics-оптимизация (mm256)    | 595553.65                     | 345           |
